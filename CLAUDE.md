# CLAUDE.md — 給 AI 助手的專案說明

> 這個檔案讓 Claude 在**任何一台電腦、任何一個新對話**都能立刻理解這個專案。
> 有重要決策或新的踩坑紀錄時，請更新這份文件。

---

## 使用者背景（很重要）

**這位使用者是第一次寫程式。** 請據此調整回答方式：

- 每個步驟都要具體：講清楚「在哪裡點」「按哪個按鈕」，不要只說「部署一下」
- 檔案路徑要完整，並說明是從專案根目錄算起
- 專有名詞第一次出現要解釋
- 每個階段都要有明確的「驗收方式」，讓他知道自己做對了沒有
- 先做出能跑的東西，再逐步加功能——不要一次丟太多概念
- 回覆使用**繁體中文**

**前一個專案是 `D:/Claude/KANTIN`（員工餐廳問題回報系統），已經上線。**
那份 `CLAUDE.md` 有 19 條設計約定與一整節常見陷阱，全部是實際踩過的坑。
**這個專案沿用同一套技術，所以那些坑會再遇到一次——動手前先去看對應章節。**

---

## 專案概述

PCI adidas 工廠的**人員出差／休假機場接送機系統**。

- 使用者（台幹及眷屬）開網頁 → 用 email 查自己的接送安排，或用日期／航班號查同班機名單
- 使用者可對自己那筆「提出額外需求」
- 管理者用 app **或直接編輯 Google Sheet** 輸入接送資料
- 超級管理者收每日系統信、發報表、管理帳號

**⚠️ 兩個入口的分工（使用者 2026-09-03 確認）：**

| 角色 | 主要在哪裡工作 |
|---|---|
| 一般管理者 | **app 頁面**——輸入、修改、查詢、刪除都在這裡。**不進 Google Sheet** |
| 超級管理者 | 以上全部，**再加上**在 Sheet 上做資料庫整理、系統信彙整 |

這件事會影響取捨：**不要再往 `onEdit` 那條路加新功能。**
它最終只剩超管一個人在走，加固一條正在被取代的輸入路徑，投資報酬是負的。
現有的自動帶入維持原狀（超管還要用），但新功能一律做成
「與呼叫端無關的核心函式 ＋ 兩個包裝」——選單一個、app API 一個。

| 項目 | 內容 |
|---|---|
| 使用者 | 台幹及其眷屬、少數訪客 |
| 資料量 | 每週約 10～25 筆，一年約 250～400 筆 |
| 管理者 | 超級管理者 **1 位**（Ken Wang）＋ 一般管理者 1～2 位 |
| 語言 | 印尼文（主）／中文（副），可切換 |
| 成本 | 零（GitHub Pro 除外，約 US$4/月） |
| 來源資料 | `airport.xls`（2026-08-25 起至 12 月，89 筆，17 個週分頁） |

**司機、保衛、接待不在使用範圍內。**

---

## 技術架構

```
GitHub Pages（前端）→ Google Apps Script（後端）→ Google Sheet（資料）
```

| 層級 | 技術 | 位置 |
|---|---|---|
| 前端 | 原生 HTML / CSS / JavaScript（不用框架） | **專案根目錄**（`index.html`、`css/`、`js/`） |
| 後端 | Google Apps Script | `gas/`（線上編輯器的備份） |
| 資料 | Google Sheet | 雲端 |

**不要引入 React / Vue / 打包工具。** 使用者是初學者，維持原生、零建置流程是刻意的決定。

**前端檔案必須放在專案根目錄，不可移到子資料夾。**
GitHub Pages 只允許 `/(root)` 或 `/docs` 兩種發布來源，而 `docs/` 已用於存放規格書。
根目錄的 `.nojekyll` 檔案不可刪除（用來關閉 Jekyll 處理）。

**repo 是 Private（GitHub Pro 方案）。**
⚠️ **但發布出去的網站仍然是公開的**——Pro 保護的是原始碼，不是網站。
要讓網站也需要登入才能看，需要 GitHub Enterprise Cloud，不是 Pro。
⚠️ Pro 是帳號層級的訂閱，**停止訂閱後 private repo 的 Pages 會停止發布，網站會掛掉。**

---

## 頁面一覽

| 檔案 | 給誰用 | 內容 |
|---|---|---|
| `index.html` | 全部 | 首頁：**查詢我的行程** ＋ **管理者專區** 兩格 ＋ 頁尾的管理者登入小連結。**全站唯一有 `PCI GA` wordmark 的頁面** |
| `query.html` | 使用者 | email 查自己 / 日期・航班號查同班機 |
| `admin.html` | 管理者 | 登入 ＋ 強制變更初始密碼（同一頁，避免按上一頁繞過） |
| `admin-list.html` | 管理者 | 班表列表、篩選、修改、批次改期 |
| `admin-roster.html` | 管理者 | 人員名冊、航班名冊維護 |
| `admin-dashboard.html` | 管理者 ＋ 超管 | 動態表（統計） |
| `admin-accounts.html` | 超級管理者 | 帳號管理 |

**管理端一律手機優先。** 管理者會在手機上輸入資料，17 欄的寬表格在手機上不能用：

| 螢幕 | 排版 |
|---|---|
| 手機 | 卡片式，一筆一張卡，重點資訊大字，其餘收在「詳細」裡 |
| 桌機 | 同一份 CSS 自動變成表格式 |

---

## Google Sheet 結構

```
📗 機場接送.gsheet
├── 25 Agust - 31 Agust  ┐
├── 01 Sep - 07 Sep      │  週分頁 ← 真實資料，格式跟 airport.xls 一模一樣
├── …（每週一張，自動建立）┘
│
├── _INDEX               ← 隱藏。程式自動攤平的衍生資料，只給查詢用
├── DAFTAR PERSONEL      ← 人員名冊
├── JADWAL PENERBANGAN   ← 航班名冊
├── PENGATURAN           ← 選項設定（廠別、部門、上車地點、系統信收件人…）
├── ADMIN                ← 管理者帳號（密碼雜湊）
└── LOG                  ← 錯誤日誌
```

### 主表欄位（週分頁）

**A～Q 欄完全比照 `airport.xls`，順序與標題一格都不能動**（只有 M 欄標題補了印尼文）：

| 欄 | 標題 | 說明 |
|---|---|---|
| A | `DATE PESAWAT` | 航班日期。送機＝起飛日、接機＝抵達日（都是「在雅加達發生的那一天」） |
| B | `JEMPUT 接 / 送 ANTAR` | 下拉：`Jemput` / `Antar`（**只寫印尼文**，表頭已經有中文了） |
| C | `廠別 FACTORY` | 名冊帶入 |
| D | `DEPT` | 名冊帶入。**整欄必須是純文字格式** |
| E | `NAME` | 名冊帶入，含 `Mr/Ms`，**不拆** |
| F | `中文名字 NAMA CINA` | 名冊帶入 |
| G | `CUSTOM` | 下拉 `YES`/`NO`。**程式完全不解讀它**，純粹存與顯示 |
| H | `手機號碼 HP` | 名冊帶入，可空白 |
| I | `出廠時間 DARI PCI` | **純文字格式**，見設計約定第 3 條 |
| J | `FLIGHT` | 打航班號 → 自動帶 K 欄 |
| K | `ETD/ETA` | 航班名冊帶入，可覆寫。一欄兩義，靠 B 欄判斷 |
| L | `DORM` | 名冊帶入（**眷屬各自的房號，不繼承**） |
| M | `上車地點 TITIK JEMPUT` | 下拉，可空白 |
| N | `郵件 EMAIL` | 名冊帶入，**允許重複** |
| O | `行李 BAGASI` | 可空白 |
| P | `POVS` | 別的系統的名稱，可空白，**程式不判斷** |
| Q | `REMARK` | **只有管理者能填**，可空白 |

**R～Y 欄是新增的，平常隱藏起來**（隱藏後畫面與列印跟 `airport.xls` 完全一樣）：

| 欄 | 名稱 | 誰填 |
|---|---|---|
| R | `booking_id` | 系統自動，格式 `AP` + 年月 + 三碼（例 `AP2609001`） |
| S | `STATUS` | 五選一，見下 |
| T | `PERMINTAAN 員工需求` | 使用者，**與 Q 欄完全分開** |
| U | `EMAIL KONTAK 聯絡人信箱` | 管理者，可不填。填了查詢時兩欄都比對 |
| V | `group_id 同行群組` | 管理者，可不填。填了統計車輛時同組算一台 |
| W | `TANGGAL ASAL 原訂日期` | 改期時自動填 |
| X | `最後更新時間` | 系統自動 |
| Y | `更新者` | 系統自動 |
| Z | `NO. KENDARAAN 車號` | 管理者，可不填 |
| AA | `SOPIR 司機` | 車輛名冊自動帶入 |
| AB | `HP SOPIR 司機電話` | 車輛名冊自動帶入 |

⚠️ **Z/AA/AB 是派車功能的欄位。v3.0 起管理者可以在 app 上填**，
顯示端（使用者卡片、管理端明細）從更早就寫好了，只是資料一直是空的所以不出現。
資料結構先準備好是因為成本不對稱：欄位當初加幾乎免費，日後才加要付一次
「所有讀主表的功能一起壞」的空窗期（設計約定第 12 條）。
這三欄標了 `optional: true`，`buildColumnMap()` 找不到就跳過不丟例外，
**所以呼叫端一定要自己檢查欄號是不是 0**——漏了檢查，`getRange(row, 0)` 會炸掉。

### STATUS 五種

| 值 | 意思 | A 欄日期 | 使用者看到 |
|---|---|---|---|
| `Terjadwal 已排定` | 預設 | 實際日期 | 完整資訊 |
| `Selesai 已完成` | 接送完成 | 實際日期 | 完整資訊 |
| `Belum Lengkap 待定` | **資訊不完整**（航班日期未定、航班號未定都算） | **暫定日期**（一定要填，見下） | 「資訊未齊，請洽窗口」，**看不到日期** |
| `Menunggu Jadwal 待改期` | 要改期，**新日期未定** | **保留原訂日期不動** | 「日期待定，請洽窗口」，**看不到日期** |
| `Batal 已取消` | 不去了 | 原日期 | 「已取消」 |

程式代碼依序是 `SCHEDULED` / `DONE` / `INCOMPLETE` / `PENDING` / `CANCELLED`。

⚠️ **「待改期」的代碼是 `PENDING`，不是新代碼。** v2.9 之前的「待定」意思就是
「要改期、新日期未定」，跟現在的「待改期」完全一樣——所以搬遷只換 Sheet 上顯示的字，
一行行為都沒改。看到 `PENDING` 要讀成「待改期」。

⚠️ **`Diundur 已改期` v2.9 起退役**，改期的事實由 W 欄（原訂日期）表達。
但 `CODE_OF` 與健檢白名單裡的 `POSTPONED` **不可以拿掉**——
搬遷刻意不動的那些列還帶著它（見下），拿掉會讓它們整批被報成「狀態是未知值」。

⚠️ **「待定」與「待改期」的 A 欄日期一定要有值、不能清空**——A 欄決定這一筆放在
哪一張週分頁，空的話它會從系統裡消失。「待定」填一個暫定日期即可
（使用者端看不到它）。

⚠️ 使用者端**不顯示這兩種狀態的日期與出車時間**，不然他會照那個日期去等車，
而那台車根本不會來。後端就清空（`gas/Query.js`），不是只靠前端不畫——
只靠前端的話，關掉 JavaScript 直接看 API 回應就露出來了。

⚠️ **使用者端一律顯示狀態徽章，包含「已排定」**（v2.9.2，使用者要求）。
沒有徽章不等於沒問題，只等於沒有訊息——一顆綠色的「已排定」是在回答
使用者真正的問題：「我的車安排好了沒有」。
配套是健檢的「已排定卻沒有出廠時間」那一條：那顆徽章等於系統承諾
「資訊都備齊了」，出廠時間空白的話使用者會照著一個空的時間出來等車。

⚠️ **五個狀態每一個都要有 `.badge--xxx` 樣式。** 漏寫不會報錯，
那顆徽章只會變成沒有背景色的灰框，看起來像壞掉但查不出原因。
`LIST_STATUS` 與 `css/style.css` 的徽章樣式要一起改。

**逾期未處理**：日期已經過了、狀態卻還是「待定」或「待改期」的，
管理端列表整列會變紅底＋一顆紅標＋統計行顯示筆數（`isOverdue()`）。
「已排定」不算——時間過了它會自己變成「已完成」，那是正常流程。

---

## 權限

| 角色 | 認證 | 能做什麼 |
|---|---|---|
| 使用者 | email（無密碼） | 查自己（email，**部分比對，最少 3 個字**）、查同班機（日期／航班號）、對自己那筆提需求。**只看得到航班日期 ≥ 今天的行程** |
| 管理者 | 帳號＋密碼 | 查詢、**修改、刪除**（v3.0，兩種管理者都有刪除權限）、新增、批次改期、名冊維護、**動態表** |
| 超級管理者 | 帳號＋密碼 | 以上全部 ＋ 帳號管理 ＋ 發送報表 ＋ 收每日系統信 |

**超級管理者只有 1 位**（使用者的決定）。配套是 `emergencyResetSuper()`：
從 Apps Script 編輯器直接執行就能重設密碼、解除鎖定、恢復啟用狀態。
⚠️ 沒有這支救援程式的話，唯一的超管忘記密碼就沒有任何人能幫他重設——
重設密碼是超管專屬功能，而他就是唯一的超管。
`guardLastActiveSuper_()` 仍然擋掉「停用最後一位啟用中的超管」。

**管理者閒置 1 小時自動登出**（v2.8，使用者指定）。三件事要一起看：

| 層 | 做什麼 | 少了會怎樣 |
|---|---|---|
| 後端 `withAuth()` | 憑證裡存「最後活動時間」，超過 `AUTH.IDLE_TTL` 就**當場刪掉**並回 `SESSION_IDLE` | 這是唯一真正的把關。前端擋不住任何人——關掉 JavaScript 直接打 API 就繞過去了 |
| 前端 `startIdleWatch()` | 盯著點擊／打字，到期前 5 分鐘跳警告條，到期就自己回登入頁 | 人離開座位後畫面還停在有資料的頁面上，共用電腦誰都看得到 |
| 登入頁 `showBounceReason()` | 用 `?idle=1` 講出「為什麼會回到這一頁」 | 使用者只看到登入畫面又出現了，第一個反應是「系統壞了」 |

⚠️ 「閒置」算的是**距離上一次呼叫後端有多久**，不是「登入了多久」。
連續工作一整天不會被踢出去，停手一小時就會。

⚠️ `AUTH.TOKEN_TTL`（8 小時）現在**只是垃圾回收的上限**，不是使用者感受得到的規則。
它一定要大於 `IDLE_TTL`，否則會變成它先到期——使用者在還沒閒置滿一小時的時候
就被登出，而且畫面上完全看不出為什麼。

⚠️ 分鐘數由後端經 `getAdminProfile` 的 `idle_minutes` 傳給前端，**前端不寫死**。
兩邊各寫一個數字，改了一邊忘了另一邊，就會出現「畫面說還有 10 分鐘，
按下去卻說登入已過期」。

**使用者端查詢不需要驗證身分，且不遮蔽他人個資**——這是使用者明確的決定，
因為「知道當天同班機有誰」是實際需求。補強措施：所有頁面加 `noindex`，讓 Google 搜尋不到。

⚠️ **但使用者端不顯示部門與手機**（v3.0，使用者要求）。那一頁不需要登入，
列出手機號碼等於把全體台幹的電話變成一份可以抄走的名單，而使用者要的只是
「我的車幾點來、在哪裡上車」。**管理端仍然顯示**——那裡要密碼才進得去，
而管理者需要手機號碼才打得到人。
後端仍然回傳這兩個欄位（管理端同一支資料來源要用），所以這是「不畫出來」不是「查不到」。

**系統信只寄給管理者與超管，不寄給使用者。** 收件人清單放在 `PENGATURAN` 分頁，**至少填 2 個**
（只有 1 個人收的話，他休假那週的待定行程就沒有人追）。

---

## 必須遵守的設計約定

> 第 1～9 條是這個專案特有的。
> 其餘沿用 `D:/Claude/KANTIN/CLAUDE.md` 的 19 條，**動手前先去看那一份**。

### 1. 週分頁是真實資料，`_INDEX` 是單向衍生，永遠不可反向寫

```
週分頁（52 張／年）  ← 真實資料，人看的、人改的、程式也寫這裡
      ↓ 程式自動攤平（只有這一個方向）
   _INDEX（1 張隱藏）  ← 衍生資料，只給查詢用
```

**這跟「雙向同步」是完全不同的東西。** `_INDEX` 壞了重算一次就好，
週分頁永遠是對的，沒有「兩邊衝突誰贏」的問題。任何時候可以整張砍掉重建。

查詢一律只讀 `_INDEX`（一次讀取），不管有 52 張還是 200 張分頁，速度都一樣。
**不可以為了省事去遍歷所有週分頁**——52 張 × 每張 getValues 約 50～200ms，光讀就要 3～10 秒。

三個配套，少一個就會壞：

| # | 配套 | 少了會怎樣 |
|---|---|---|
| 1 | **每天 03:00 自動建好本週與未來三週**（`ensureUpcomingWeekSheets`） | 新的一週沒人建分頁，程式沒有地方寫。而人**手動建的分頁程式認不得**，見第 7b 條 |
| 2 | **分頁日期靠每列的 A 欄判斷，不靠分頁名稱** | 現有名稱格式就不一致（`25 Agust - 31 Agust`、`22 Sep-28 Sep`、`13 Okt- 19 Okt`），靠名稱解析必然會壞 |
| 3 | **改期跨週要搬分頁** | 09/18 改成 09/25 要從第 38 週搬到第 39 週。app 上改會自動搬；手動改的話每日重建會自己修正 |

**每天凌晨整張 `_INDEX` 重建一次**（自我修復，抓漏）。

### 2. 日期一律 `dd/mm/yyyy`，時間一律 24 小時，年份一律西元

⚠️ **來源資料是 `mm/dd`，跟新規則剛好相反。** 而且這個混淆已經造成過錯誤資料：
有人寫 `03/09 20:00`（想表達 9 月 3 日），Excel 照 mm/dd 讀成 2026 年 3 月 9 日，
於是 9 月的分頁裡躺著兩筆 3 月的資料。轉檔程式已修正這兩筆。

**新資料一律 dd/mm。** 讀取手寫字串時也一律以 dd/mm 解讀。

### 3. `出廠時間 DARI PCI` 欄是**純文字**格式

顯示規則：

| 情況 | 存的內容 |
|---|---|
| 航班當天出車 | `20:00` |
| 前一天出車 | `30/08 20:00` |

Google Sheet 的儲存格格式**做不到**「同一天只顯示時間、跨天才顯示日期」——
數字格式不能去參考別的儲存格（A 欄的航班日期）。所以這一欄存純文字，程式讀取時再解析。

⚠️ **整欄必須先設成「純文字」格式再寫值**（KANTIN 設計約定第 11 條）。
沒設的話 Sheet 會自作主張判斷型別，`03/09 20:00` 又會變成 3 月 9 日——**那個 bug 就是這樣來的**。

程式的容錯解析順序（`gas/Utils.js` 的 `parseDariPci_`）：

| 輸入 | 解讀 |
|---|---|
| `20:00` | 航班當天 20:00 |
| `30/08 20:00`、`30/8 20:00`、`30-08 20:00` | 8 月 30 日 20:00 |
| 看不懂的 | 列進每日系統信的「資料異常」段，不猜 |

### 4. 主表存「快照」，不存指向名冊的關聯欄位

新增行程的那一刻，姓名、部門、廠別、房號、手機就直接寫進主表的 C～L 欄。
**主表沒有 `person_id`。**

好處：

- 名冊怎麼改、怎麼停用、甚至直接在 Sheet 上刪列，**歷史紀錄一筆都不會壞**
- 某人 9 月住 R6-1、10 月搬到 R8-2，9 月那筆紀錄就該是 R6-1（這是對的）

代價：打錯字要修正時，舊紀錄不會跟著改。所以**改名冊時要跳提示讓使用者選**：
只影響之後新增的（預設）／一併更新未來的／一併更新全部的。
比對用 `email + NAME`（同一個 email 底下姓名必不相同，所以這組合是唯一的）。

### 5. 名冊一律「停用」，不做「刪除」按鈕

`AKTIF 啟用` 改成否 → 輸入新資料時不出現在候選清單，歷史紀錄照樣查得到。
**停用是可逆的**（訪客再度來訪、員工回鍋、航班復飛），名冊頁有「顯示已停用」開關。

真的建錯一筆重複的，直接在 Sheet 上刪列是安全的（因為第 4 條）。

### 6. 一個 email 可以對應多個人，帶入時「查到多筆就讓使用者選」

眷屬與員工**共用同一個 email**（這是刻意的設計，來源資料本來就是這樣）。

```
管理者輸入 linda.lim@pci.co.id
   → 找到 3 位 → 選 Bryant
   → 姓名、部門、廠別、房號 R6-2、手機 一次帶入
```

**只找到 1 位就直接帶入，不跳選單**（89 筆裡有 85 筆是這種情況）。

⚠️ **房號不可繼承。** 實際資料裡 Linda 一家三口是 R6-1 / R6-2 / R6-3 三間不同的房。
名冊裡每個人一列、各自存自己的房號。其餘欄位（部門、廠別、手機）確實相同。

**輸入框同時支援 email、中文名、英文名三種**，打 `林麗` 或 `Bryant` 都能跳出候選。

### 7. 兩個寫入口（app ＋ 直接編輯 Sheet）的五個對策

這是本專案跟 KANTIN 最大的不同，也是最容易壞的地方。

| # | 對策 | 解決什麼 |
|---|---|---|
| 1 | **自動補號**：觸發器發現有列缺 `booking_id` / `最後更新時間` 就補上 | 手動新增的列沒有編號，程式抓不到它 |
| 2 | **Sheet 上直接設「資料驗證」**：接／送、廠別、狀態、上車地點都做成下拉選單 | 手打的錯字（`Jemput` / `jemput` / `接機`）會讓統計整個歪掉。在源頭擋住比事後清理有效一百倍 |
| 3 | **保護表頭列**（Google Sheet 的「保護範圍」） | 表頭被改動，`buildColumnMap()` 找不到欄位會丟例外，**所有功能一起壞**（KANTIN 約定第 12 條） |
| 4 | **每日資料健檢**，寫進系統信 | 手動輸入的錯誤一定會發生。重點不是防止，是**讓它在害到人之前被發現** |
| 5 | **寫入時上鎖**（`LockService`） | app 寫入的同時有人在 Sheet 上編輯，兩邊會互相覆蓋。人少不代表不會撞——兩個入口本來就同時存在 |

兩條靠約定、技術擋不住的（要寫進管理者手冊）：

- ❌ 不要在 Sheet 上直接刪主表的列 → 改把 `STATUS` 設成「已取消」
- ❌ 不要新增／刪除／搬動欄位 → 要加欄位得改程式（KANTIN 約定第 12 條有兩種安全作法）

### 7b. ⚠️ 手動建立的週分頁，程式認不得

**這是最危險的一種失效，因為它完全不報錯。**

程式靠分頁的 developer metadata（`weekStart`）認出週分頁，**不是靠名稱**。
所以複製一個分頁出來當新的一週，會發生這種連鎖失效：

```
手動複製分頁 → 沒有 weekStart 標記 → isWeekSheet_() 回 false
  → onEdit 不處理它（不自動帶入、不編 booking_id、不標記索引待更新）
  → 索引永遠不重建 → app 查不到那一整週
```

而 Sheet 上看起來一切正常。三道防線：

| # | 機制 | 時機 |
|---|---|---|
| 1 | `ensureUpcomingWeekSheets()` | 每天 03:00 自動建好本週與未來三週，**讓人根本不必手動建** |
| 2 | `adoptOrphanWeekSheets_()` | `onEdit` 當場認養 ＋ `rebuildIndexIfDirty` 每 5 分鐘掃一次 |
| 3 | `checkData()` / `repairWeekSheets()` | 手動檢查與修復 |

⚠️ 認養靠**第一列有日期的資料**推算週次，不是分頁名稱——名稱是人取的，可能打錯或忘了改。

⚠️ 第 2 道的掃描要放在「有沒有變更」的判斷**之前**。
複製了分頁但還沒編輯過時旗標不會立起來，只在有變更時才掃的話，那張分頁永遠不會被認養。


### 8. 前端快取：名冊有個資，只能存 sessionStorage

管理者輸入時要「打字就即時跳候選」，所以名冊必須快取在裝置上
（不然每打一個字打一次 API，Apps Script 每次 3～8 秒，根本不能用）。

依 KANTIN 設計約定第 18 條的判斷標準——「能不能存在裝置上，看的是洩漏出去會怎樣」：

| 存什麼 | 存在哪 | 為什麼 |
|---|---|---|
| 管理者 token | `sessionStorage` | 關掉分頁就失效（KANTIN 約定第 9 條）。**另有閒置 1 小時自動登出**，見權限那一節 |
| **人員名冊** | **`sessionStorage`** | **含全體台幹的手機與房號。** 手機借人、掉了、共用電腦——關掉分頁就沒了 |
| 航班名冊 | `localStorage` | 沒有個資，而且幾乎不會變，適合長期快取 |
| 選項設定 | `localStorage` | 沒有個資 |

⚠️ 所有讀寫都要包 `try/catch`。無痕視窗、空間滿了、使用者關掉網站資料，`setItem` 都會丟例外。
存不進去只是下次要重抓，沒包起來的話整個表單會白畫面。

### 9. 版面只有三個斷點，不要再加第四個

| 斷點 | 為了什麼 | 動到誰 |
|---|---|---|
| `max-width: 420px` | 頂列塞不下時的取捨：**標題永遠留著**，犧牲返回鍵的文字（只留箭頭） | `.topbar` |
| `max-width: 760px` / `min-width: 761px` | 班表列表在這裡**從表格變成卡片** | `.dtable` |
| `print` | 列印時把操作元件全部藏起來 | 全站 |

斷點一多就會出現「某個寬度剛好卡在兩條規則中間」的怪畫面，而且沒有人會去測那個寬度。
要加第四個之前，先確認現有三個真的解不了。

⚠️ **斷點一律寫成 `@media screen and (...)`。** 少了 `screen`，列印時會被誤觸發。

**三條「一次擋掉一整類問題」的防禦規則**（`css/style.css` 最上面）——
新頁面自動受保護，不必逐頁調：

| # | 規則 | 少了會怎樣 |
|---|---|---|
| 1 | `img, svg, video, canvas, table, pre { max-width: 100% }` | 比容器寬的圖把整頁撐開，出現橫向捲動而且看不出是誰造成的 |
| 2 | 文字容器 `overflow-wrap: break-word` | 沒有空格的長字串（email、貼上的網址）撐破卡片 |
| 3 | flex／grid 子元素 `min-width: 0` | **最容易漏的一條**，見下 |

⚠️ 第 3 條要**逐個列出容器**，不能只寫一個。`min-width: auto` 是每一個 flex／grid
子元素的預設值，意思是「不會縮到內容寬度以下」。而 `overflow-wrap` 救不了它——
換行是「行」層級的事，`min-content` 寬度是「格子」層級的事，**格子撐開了就沒有行可以換**。
v2.4 抓到三處：`.topbar .brand`（長標題把語言切換擠出畫面）、
`.kv dd`（一個長 email 把值那一欄從 200px 撐到 490px）、
`.dtable td`（卡片模式下姓名太長時右邊的標籤被推出畫面，用 `flex-wrap: wrap` 解）。

**驗證方式**：開發端有一支自動版面檢查（5 頁 × 320/390/1280 ＋ 班表列表在 759/761），
每頁驗「沒有橫向捲動 / 點擊目標 ≥ 44px / 沒有元素超出畫面」。
⚠️ 它需要 Playwright（約 300MB），**刻意不放進 `tools/`**——
使用者端要維持零建置流程，`tools/check.js` 只做五項靜態檢查。

---

## 常見陷阱

| 陷阱 | 說明 |
|---|---|
| **時區有三個地方要設** | ① Google Sheet：檔案→設定→地區 `Indonesia`、時區 `(GMT+07:00) Jakarta` ② Apps Script 專案時區 `Asia/Jakarta` ③ 前端**不做時區換算**。漏了任一個都會**安靜出錯**：Sheet 用 mm/dd 顯示、程式寫進去的時間差一小時、台灣的手機看到的時間跟印尼的不一樣 |
| **後端一律傳「已格式化好的字串」** | 傳 `30/08/2026 20:00`，不要傳 ISO 讓前端自己 `toLocaleString()`。JS 的 `new Date()` 用的是看的人那台裝置的時區，台灣的手機會多算一小時 |
| `datetime-local` 的顯示順序管不了 | 跟著手機系統地區設定走，HTML 和 CSS 都改不了。**解法：輸入框下方即時顯示一行 `28/08/2026 20:00` 確認文字**，那一行由我們控制。（使用者明確表示**不要顯示星期**） |
| **改 JS 時不要用 shell heredoc 寫含跳脫序列的字串** | 實際踩過兩次：heredoc 裡的換行跳脫序列會被寫成**真實的換行**，字串就跨行了，出現 `SyntaxError: Invalid or unexpected token`。改用 Write / Edit 工具直接寫檔，或在程式裡用 `String.fromCharCode(10)` 搭配陣列 join。⚠️ `tools/check.js` 的語法檢查抓得到它，發布前一定會擋下來 |
| **發布後「怎麼沒變」** | **先懷疑快取，不要改程式。** GitHub Pages 對**所有檔案**送 `Cache-Control: max-age=600`（實測確認），**HTML 檔案也一樣**。而 `?v=` 版本號只掛在 CSS/JS 上——瀏覽器若還留著舊的 `admin.html`，它就會去載舊版本號的 JS，於是新功能完全不出現。<br>驗證方式：用**沒有你快取的環境**看一次（無痕視窗，或請別人開）。若那邊是新的，就是快取，**Ctrl+F5 或等 10 分鐘**即可，程式沒有問題。 |
| **前端「今天」要換算成雅加達** | 「前端不做時區換算」只適用於**顯示後端傳來的值**（那些已經格式化好了）。但前端自己算日期時（`js/admin-list.js` 快捷區間的本週／未來 14 天）**一定要換算**：台灣的手機比雅加達快一小時，台北 00:30 時雅加達還在前一天 23:30，直接用裝置日期的話那一個小時裡整段位移一天，而且畫面上完全看不出哪裡不對。作法見 `todayJakarta()`——印尼西部時間固定 UTC+7 且沒有日光節約，直接位移就是準的 |
| 手機版 CSS 斷點 | 一律寫成 `@media screen and (max-width: ...)`。少了 `screen` 的話列印時會被誤觸發。**斷點只有三個，加第四個之前先看設計約定第 9 條** |
| **改了 `LIST_xxx` 卻沒重裝下拉選單** | 實際踩過（v2.9 上線）。Sheet 上的下拉選單是「裝上去的」，不是每次讀 Config 現算的。程式改了、Sheet 上那份沒換，兩邊就對不起來——而狀態與接／送是**嚴格驗證**（`setAllowInvalid(false)`），選單裡沒有的值**管理者選不到也打不進去**。<br>當時重裝選單掛在搬遷的「有資料要搬才做」裡面，而實際搬遷是 0 筆（匯進來的資料本來就沒有舊值），於是一次都沒跑到，**新的兩個狀態在 Sheet 上完全用不了，而且沒有任何錯誤訊息**。<br>**作法**：① 改過任何 `LIST_xxx` 就跑選單的「重設下拉選單」② 會動到選項的搬遷程式，重裝選單要放在「不論有沒有改到資料都做」的位置③ 空白的週分頁也要重裝——那正是接下來要輸入資料的地方 |
| **class 掛上去了但顏色沒變** | 實際踩過**三次**（v2.7 的 `.chips`、v2.9 的 `.statline--alert`、v3.1 的 `.entry--primary`）。權重算錯——`.statline--alert` 是 0,1,0，輸給既有的 `.listbar .count`（0,2,0），於是 class 確實在元素上、顏色卻是舊的。**畫面正常、沒有錯誤訊息。**<br>**作法：新增的狀態 class 要跟它要覆蓋的那條寫成同樣或更高的權重**（這裡用 `.count.statline--alert`，兩個 class 疊起來就是 0,2,0，再靠檔案位置在後面贏）。<br>**v3.1 的變形：權重一樣、但 modifier 寫在基本規則前面。** `.entry--primary .entry-note` 與 `.entry .entry-note` 都是 0,2,0，權重一樣時是「檔案後面的贏」——寫在前面就被蓋回去了。<br>**新增 modifier class（`--primary`、`--alert`、`--overdue`）時先問一句：它要覆蓋的那條寫在哪裡？寫在它後面。**<br>⚠️ 光測「class 有沒有掛上去」抓不到這種問題——一定要量 `getComputedStyle` 的實際值 |
| **媒體查詢裡的規則沒生效** | 實際踩過（v2.7 的 `.chips` 3 欄格線）。**媒體查詢不會增加權重**——`@media` 裡的 `.chips` 跟外面的 `.chips` 權重完全一樣，權重一樣時是**檔案後面的贏**。把 `@media` 區塊寫在基本宣告**前面**，基本宣告就會把它蓋掉，而且畫面正常、沒有任何錯誤訊息，只是「新規則沒作用」。<br>**作法：媒體查詢一律放在它要覆蓋的那條基本宣告之後。**（不是新增斷點，同一個斷點在檔案裡出現兩次是正常的，設計約定第 9 條算的是斷點**數量**，不是區塊數量） |
| **程式化編輯把 HTML 從中間截斷** | 實際踩過（v3.0）。用程式插入區塊時切片少接了尾巴，`</main>` 之後的頁尾、**所有 `<script>` 標籤**、`</body></html>` 整段不見。**當時所有檢查都是綠的**——語法檢查只看 `.js`，版本號檢查只數 `?v=`（剩下的剛好還在），翻譯檢查只掃 `data-i18n`。而瀏覽器對半截的 HTML 完全不抱怨，它只是安靜地不載入任何 JavaScript：畫面長得幾乎一樣，按什麼都沒反應。<br>**作法**：`tools/check.js` 的 `checkHtmlComplete()` 會擋（結尾、`<script>` 數量、標籤開合）。改 HTML 後**一定要跑一次 `node tools/check.js`** |
| **測試綁在欄位位置上** | 實際踩過（v3.1 把班表列表 8 欄併成 6 欄）。`test-page.js` 用 `td:nth-child(3)` 取姓名，欄位一合併就讀到「出廠時間」那一格，**五個排序測試同時變紅，而排序本身完全沒壞**——花時間找的是不存在的 bug。<br>**作法：測試一律用語意化的選擇器**（`.rname`、`[data-label="出廠時間"]`），不要用位置。這跟後端「欄位一律問表頭、不可以用定義順序」是同一個道理。 |
| **量表格儲存格的高度測不出折行** | 實際踩過（v3.1）。同一列裡每一格的高度都相等，會被最高的那一格撐開，量出來永遠一樣。<br>要數行數得用 `Range.getClientRects()`，⚠️ 但它回的是**行內盒**不是行數——一行裡有兩個 `<span>` 就回兩個矩形。**要數有幾個不同的 `top`。** |
| **翻譯字串裡寫 markdown** | 實際踩過兩次（v2.5、v3.0）。翻譯是**純文字**，用 `textContent` 塞進畫面——寫 `**粗體**` 的話星號會原樣印在使用者眼前。程式碼註解裡這樣寫是習慣，順手就帶進字串裡了，而畫面看起來「只是多了幾個星號」，不像壞掉。<br>`tools/check.js` 現在會擋含 `**` 的翻譯字串 |
| **pointerdown 會讓按鈕按不到** | 實際踩過（v2.8 的閒置警告條）。全頁監聽 `pointerdown` 當作「有動作」，而處理函式把警告條 `remove()` 掉——手指按下去的那一瞬間按鈕就從 DOM 消失了，**等 click 要送達時已經沒有那顆按鈕**，於是它永遠按不到。而畫面看起來完全正常（警告條確實收起來了），只有按鈕該做的事安靜地不發生。<br>**兩個作法都要**：① 全頁監聽要跳過那一塊（`e.target.closest('#idleBar')` 就 return）② 收起來用 `hidden` 不要用 `remove()` |
| **iOS 的 `input[type="date"]`** | 它是**原生控制項**，有自己的固有寬度，`width: 100%` 對它無效，所以會撐出卡片外、文字還被置中。**Chromium 重現不出來**，只有 iOS Safari 會壞。解法是 `-webkit-appearance: none`（把它降級成一般輸入框，CSS 寬度才重新生效），再補 `min-width: 0`（flex 裡縮得下去）與 `text-align: left` |
| `<input type="date">` 的顯示順序 | **跟著看的人那台裝置的地區設定走，HTML 和 CSS 都改不了**：印尼手機顯示 `18/09/2026`、美式設定的電腦顯示 `09/18/2026`。但送出去的值永遠是 `YYYY-MM-DD`（HTML 規格規定），所以**查詢結果一定是對的，差別純粹在畫面上**。解法：輸入框下方放一行我們自己控制的確認文字（`js/query.js` 的 `showPickedDate`），一定是 dd/mm/yyyy |
| 後端的錯誤訊息 | 回**代碼**不回句子（`EMAIL_TOO_SHORT`），翻譯在 `js/i18n.js` 的 `tError()`。後端不知道使用者把介面切成哪種語言，回中文句子的話印尼籍使用者會看到一句看不懂的話 |
| 標點符號要跟著語言 | 中文用全形「：」，印尼文用半形「: 」。用 `js/i18n.js` 的 `colon()`，不要寫死 |
| **拿掉一個欄位比加一個危險** | 實際踩過（v2.3 移除人員名冊的通知信箱欄）。兩個安靜失效點：① 讀取端若用**定義順序**對應欄位（`PERSON_COLUMNS.forEach((c,i)=>...)`），只要程式和 Sheet 有一邊先變，整排就位移一欄——被拿掉的那欄剛好在 `AKTIF` 左邊，於是 `CODE_OF['']` 不等於 `'N'`，**已停用的人全部重新出現在候選清單裡**，畫面上完全看不出來。② `applyStructure_()` 的表頭是**按位置**寫的，定義少一欄而 Sheet 上那欄還在的話，`setupSheet()` 會把右邊那欄的標題蓋到它上面，變成兩個同名欄。<br>**作法**：先把讀寫端全部改成 `buildColumnMap_()`（照表頭名稱找欄），順序問題就消失了；再用一支**按名稱刪欄**的函式（`dropRetiredPersonColumns_()`）處理實體欄，放在 `setupSheet()` 最前面。不要叫人自己在 Sheet 上右鍵刪欄——刪錯一格就是資料不見了 |
| DEPT 欄型別 | 來源資料 70 筆是數字、19 筆是文字（因為 `502Z039000` 有字母）。同一欄兩種型別，篩選部門會漏資料。**整欄設純文字格式再貼** |
| 航班號的空格 | 來源有 `CZ 8056`（中間）和 ` CZ8353`（開頭）。寫入前一律去空格並轉大寫 |
| 跨午夜航班 | 8 筆凌晨起飛（`CZ8354` 00:20、`CX798` 00:05、`CI764` 01:00、`CX796` 00:15）。**表上是隔天，車子前一天晚上就要出發**。「今天要出車的名單」必須用出廠時間篩選，不能用航班日期 |
| Apps Script 重新部署 | 必須用「管理部署作業 → 鉛筆 → 版本選『新增版本』」。按「新增部署作業」會產生新網址，前端就斷了 |
| 部署權限 | 「誰可以存取」必須選**所有人**，不是「所有 Google 帳號使用者」（使用者不會登入 Google） |
| 新增排程之後 | **一定要重跑 `installTriggers()`。** `clasp push` 只是把程式碼推上去，Google 的鬧鐘不會自己出現 |
| 第一次用新服務（寄信、Drive、觸發器） | **第一次執行一定會失敗一次。** Apps Script 靠掃描程式碼推算需要哪些權限，剛推上新程式碼時可能還在用舊的權限清單。再執行一次就好 |
| Windows PowerShell | `clasp` / `npm` 要打 `clasp.cmd` / `npm.cmd`（未簽章的 `.ps1` 被執行原則擋掉）。`git` / `node` 不用 |
| 個資檔案 | `.gitignore` 已排除 `*.xls` / `*.xlsx` / `*.csv` / `data/`。**就算 repo 是 private 也不上傳**（git 歷史是永久的） |

---

## 重要文件

| 檔案 | 內容 |
|---|---|
| `docs/規格書_v1.md` | **目前依據的規格**，所有設計決策的來源 |
| `docs/部署筆記.md` | 所有網址、ID、部署步驟、GitHub Pro 訂閱狀態 |
| `docs/開發進度.md` | 目前做到哪裡 |
| `data/轉檔檢查報告.txt` | 轉檔修正了什麼、哪些需要人工確認（**不進 git**） |

⚠️ **`data/` 底下的 CSV 是一次性的匯入產物，不是正在使用的資料。**
真正的資料只在 Google 試算表上。改那些 CSV 不會有任何效果，
也不要拿它們去跟 Sheet 比對——它們是匯入當天的舊資料。
（`data/README.txt` 有同一段說明，因為那個資料夾不進 git，換電腦時看不到這裡。）

---

## 開發環境

### 一次性轉檔（已完成）

```bash
python tools/convert.py
```

讀 `airport.xls` 產出 `data/` 底下三份 CSV 與一份檢查報告。**只讀不寫，不會動到 airport.xls。**
需要 `xlrd`（`python -m pip install xlrd`）。

⚠️ 這是專案裡唯一的 Python 程式，因為 `airport.xls` 是 1997 格式、Node 要讀它得另外裝套件，
而這是跑一次就不再用到的工作。**日後的常態工具一律用 Node。**

### 前端預覽

```bash
node tools/dev-server.js
```

### 檢查（隨時可跑，不動任何檔案）

```bash
node tools/check.js
```

五項檢查，每一項都對應一個實際踩過的坑：語法、**重複的函式名稱**、
**隱形的控制字元**、版本號一致、個資檔案有沒有被 `.gitignore` 擋住。

### 發布（一個指令做完整套）

```bash
node tools/release.js 0.3 "說明"
```

依序做：改版本號（三處）→ 跑 `check.js` → git commit/push → `clasp push` → `redeploy`。
**任何一步失敗就整個停下來。** 加 `--dry-run` 可以先看它要做什麼。

⚠️ **不要做成「存檔就自動推」。** 網站是即時上線的，那等於每按一次 Ctrl+S
就把改到一半的東西送到使用者面前，git 歷史也會變成一堆沒有意義的紀錄。
發布是一個刻意的動作——所以這支要求你自己給版本號和說明。

⚠️ **版本號一定要往上加。** GitHub Pages 讓瀏覽器把 CSS/JS 快取 10 分鐘，
版本號沒變的話使用者拿到的是舊 JS 配新 API，畫面會用「錯誤的方式」壞掉
（設計約定第 5 條）。`release.js` 會擋掉「版本號沒變」的情況。

⚠️ 部署 ID 從 `js/config.js` 的 `API_URL` 推導，**不另外存一份**——複製一份必然會走鐘。

### 手動操作後端（很少用到）

```bash
clasp.cmd push -f
```

```bash
clasp.cmd redeploy <deploymentId> -d "說明"
```

### 修改後端的標準流程

1. 改 `gas/` 底下的檔案
2. 串接檢查（模擬 Apps Script 共用全域範圍，可抓出重複宣告）：
   ```bash
   cat gas/*.js > tmp-all.js && node --check tmp-all.js && rm tmp-all.js
   ```
   ⚠️ 不要寫成 `cat gas/Config.js gas/*.js`——`gas/*.js` 已包含前者，串兩次必然報
   `Identifier has already been declared`，看起來像程式有錯，其實是指令本身的問題。

   `node --check` 抓得到重複的 `const`，**但抓不到重複的 `function`**。新增函式時順手跑：
   ```bash
   grep -oh "^function [a-zA-Z0-9_]*" gas/*.js | sort | uniq -d
   ```

   ⚠️ **`node --check` 也抓不到字串裡的控制字元。** 實際踩過：某個字串字面值裡的空格
   變成了 NUL（`'\x00'`），Node 的 V8 容忍它、檢查全綠，貼進 Apps Script 卻直接
   `SyntaxError: Invalid or unexpected token`。編輯器上完全看不出來，因為它不顯示。
   改完程式一併跑這一支：

   ```bash
   python -c "import glob;[print('⚠',p,i,hex(ord(c))) for p in glob.glob('gas/*.js') for i,l in enumerate(open(p,encoding='utf-8'),1) for c in l if ord(c)<32 and c not in '\t\n\r']"
   ```
3. `clasp.cmd push -f`
4. `clasp.cmd redeploy <部署ID> -d "說明"`
5. 用 curl 打 API 驗證

⚠️ 測試檔裡**不要寫死絕對路徑**（用 `path.join(__dirname, '..')`）。

---

## 開發原則

**先打通一條最小可行路徑，再擴充功能。**

每個階段都要做到「可以實際使用」再進下一階段：

| 階段 | 內容 | 驗收 |
|---|---|---|
| 0 | Sheet 結構 ＋ 轉檔 ＋ 兩份名冊 | Sheet 上看到 89 筆乾淨資料，凌晨航班的日期是對的 |
| 1 | `query.html` 使用者查詢（唯讀） | 用自己的 email 查，看得到接送紀錄 |
| 2 | 管理者登入 ＋ 班表列表 ＋ 新增／修改 | 從 app 新增一筆，Sheet 正確的週分頁上出現；在 Sheet 改一筆，app 跟著變 |
| 3 | 兩個入口的五個對策 | 故意在 Sheet 亂打一筆，隔天系統信指出那一筆 |
| 4 | 每日系統信（本週動態＋待定提醒＋資料健檢） | 收到信，內容正確 |
| 5 | 動態表 ＋ 帳號管理 ＋ 每月備份 | — |
