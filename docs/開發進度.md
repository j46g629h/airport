# 開發進度

> **接手的人先看這一份，再看 `CLAUDE.md`（設計約定與踩過的坑）。**

---

## 目前狀態（2026-09-03，v1.6 上線）

| 項目 | 狀態 |
|---|---|
| 網站 | https://j46g629h.github.io/airport/ |
| repo | https://github.com/j46g629h/airport（**Public**，免費方案） |
| 試算表 | Google 雲端硬碟裡的「**機場接送系統 Jadwal Bandara PCI**」 |
| 後端 API | 已部署，`/exec` 與部署 ID 見 `docs/部署筆記.md` |
| 資料 | 89 筆接送、52 筆人員名冊、25 筆航班名冊、28 個週分頁（半年份） |
| 觸發器 | **4 個**（見下方） |
| 發布方式 | `node tools/release.js <版本> "<說明>"` |

**使用者查詢頁已經上線給台幹用了。** 管理端只做完登入與帳號管理。

---

## 完成了什麼

### 階段 0 — 資料與結構 ✅

- `tools/convert.py` 把 `airport.xls`（17 個週分頁、89 筆）轉進新結構
- 修掉 5 個資料問題：2 筆 Excel 日月顛倒誤判、2 筆 mm/dd 手寫日期、1 個航班號空格
- Google Sheet 結構、格式、下拉選單、表頭保護

### 階段 1 — 使用者查詢 ✅

- `query.html`：email（部分比對，最少 3 字）／航班日期／航班號，三種查法
- 只顯示**航班日期 ≥ 今天**的行程
- 雙語（印尼文主、中文副）、手機優先、25 秒逾時＋自動重試一次

### 階段 2a — 管理者登入 ✅

- 加鹽 SHA-256 迭代 1000 次、token 存 `sessionStorage`（6 小時）
- 失敗 5 次鎖 15 分鐘
- 「忘記密碼」寄臨時密碼——**臨時密碼是額外一組，不會動到現有密碼**（見 `gas/Auth.js` 檔頭）
- 登入 ／ 忘記密碼 ／ 強制改密碼**同一頁**（避免按上一頁繞過）
- `emergencyResetSuper()` 救援程式（只有 1 位超管的配套）

### 階段 2b — 帳號管理 ✅

- 新增／停用／改角色／重設密碼／解鎖／刪除
- 三條安全規則（不能對自己、不能動最後一位超管、不能重設自己）
- 刪除前把整列寫進 `LOG`

### Sheet 直接輸入的保護 ✅（不在原計畫內，實際使用時補的）

- 打 email 或姓名自動帶入名冊資料；清空自動收回
- `booking_id`、`person_id`、`kendaraan_id` 自動編號
- 航班號自動轉大寫去空格、**重複時紅底＋註解**
- 週分頁每天自動建好半年份
- **孤兒分頁自動認養**（手動建的分頁程式認不得，見 `CLAUDE.md` 第 7b 條）
- `checkData()` 資料健檢、`cleanupGhostRows()` 清殘留列
- 試算表上的 **🛫 機場接送** 自訂選單

---

## 還沒做（接下來的順序）

### 階段 2c — 班表列表與篩選 ← **下一步**

| 檔案 | 內容 |
|---|---|
| `gas/Bookings.js` 擴充 | 管理端查詢（**沒有「今天以後」的限制**，看得到全部歷史） |
| `admin-list.html` ＋ `js/admin-list.js` | 手機卡片式／桌機表格式 |

篩選：日期區間、接／送、廠別、部門、姓名或 email、航班號、狀態、**有未處理的員工需求**。

### 階段 2d — 新增與修改

- 寫入 API：新增、修改、取消、**刪除**（刪除前寫 LOG）
- **改日期跨週要搬分頁**（最容易出錯的地方）
- 表單：打 email 帶入、打航班號帶時間、日期時間選擇器
- 改名冊時問「要不要一併更新既有紀錄」（現在還沒有，打錯字要手動兩邊各改一次）

### 階段 2e — 批次改期

日期／航班號／出廠時間／狀態，四個欄位。改航班號時自動帶入新的起降時間。

### 階段 3 — `_INDEX` 增量更新

現在是每 5 分鐘整張重建（19～24 秒）。改成「改哪一列就更新哪一列」。

### 階段 4 — 每日系統信

本週動態 ＋ 今明日出車 ＋ **資料健檢（`checkData()` 已經寫好，直接接上去）** ＋ 新需求 ＋ 系統錯誤。
收件人在 `PENGATURAN` 分頁的 `EMAIL_SISTEM`（**目前是空的，要填**）。

### 階段 5 — 動態表、每月備份

### 未定 — 派車功能

**資料層已完成**（主表 Z/AA/AB 三欄、`DAFTAR KENDARAAN` 分頁、自動帶入司機電話、查詢 API 已回傳）。
只剩管理者的輸入頁面與前端顯示。使用者說上線後看狀況再決定要不要啟用。

---

## 目前安裝的 4 個觸發器

| 函式 | 時機 | 用途 |
|---|---|---|
| `rebuildIndexIfDirty` | 每 5 分鐘 | 有人改過才重建 `_INDEX`（無條件重建會超出每日配額） |
| `markPastAsDone` | 每小時 | 時間過了的「已排定」改成「已完成」 |
| `ensureUpcomingWeekSheets` | 每天 03:00 | 建好從今天起半年份的週分頁 |
| `onSheetChange` | 結構變動時 | 接住插入列／刪除列（**`onEdit` 對這些不觸發**） |

⚠️ 新增觸發器之後**一定要重跑 `installTriggers()`**，`clasp push` 不會讓 Google 的鬧鐘自己出現。

---

## 維護用的函式（都在試算表的 🛫 選單裡）

| 函式 | 用途 |
|---|---|
| `checkData()` | 資料健檢，**只讀不寫**，隨時可跑 |
| `rebuildIndex()` | 立刻重建索引（不想等 5 分鐘時） |
| `ensureUpcomingWeekSheets()` | 補齊未來半年的週分頁（單次上限 6 個） |
| `cleanupGhostRows()` | 清掉「A~Q 全空但隱藏欄有值」的殘留列 |
| `repairWeekSheets()` | 認養孤兒分頁 ＋ 補格式 ＋ 排序 |

編輯器裡才有的（選單上沒放）：

| 函式 | 用途 |
|---|---|
| `emergencyResetSuper()` | ⚠️ 忘記密碼時的救命繩。重設超管密碼、解除鎖定、恢復啟用 |
| `backfillBookingIds()` / `backfillRosterIds()` | 補編號（一次貼超過 200 列時會用到） |
| `addWeekSheetForDate()` | 建立半年以外的某一週（先改 `TARGET_WEEK_DATE`） |
| `checkSetup()` | 印出試算表整體狀態 |
| `listTriggers()` | 確認觸發器數量 |

---

## 使用者還沒處理的事

| # | 事項 |
|---|---|
| 1 | 人員名冊補到 100 筆、航班名冊補到 60 筆（目前 52 / 25） |
| 2 | `data/轉檔檢查報告.txt` 的 13 項待確認，最重要的是 **`Mr Fankie Lin` 與 `Mr Frankie Lin` 是同一個人**，要刪掉其中一筆 |
| 3 | `PENGATURAN` 的 `EMAIL_SISTEM` 是空的（階段 4 的系統信要用） |
| 4 | `_IMPORT` 分頁（0 筆）可以刪掉 |
| 5 | 決定要不要做「每天自動健檢、有問題才寄信」（`checkData()` 已寫好，接上去即可） |

---

## 變更紀錄

| 版本 | 內容 |
|---|---|
| v1.6 | 接住插入列／刪除列：修正索引不更新與航班重複標記殘留 |
| v1.5 | 航班重複警告、開啟時跳到本週、試算表自訂選單 |
| v1.4 | 修正 `checkSetup` 的列數計算；殘留列清理 |
| v1.3 | 認養孤兒分頁改為失敗不影響索引重建 |
| v1.2 | 週分頁自動建立延長為半年份 |
| v1.1 | 名冊納入資料健檢 |
| v1.0 | 手動輸入的保護：自動編號、週分頁自動建立與認養、資料健檢 |
| v0.9 | 階段 2b 帳號管理 |
| v0.7 | 階段 2a 管理者登入 |
| v0.6 | 時間過了自動顯示已完成、查過去日期給明確訊息 |
| v0.3 | 發布工具、日期確認文字、email 部分比對、只顯示今天以後 |
| v0.1 | 階段 1 使用者查詢上線 |
