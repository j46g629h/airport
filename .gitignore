# ── 個資：絕對不可上傳 ────────────────────────────────
# 就算 repo 是 private 也不上傳。理由：
#   1. 「現在是 private」不代表永遠（日後可能改公開、可能加協作者）
#   2. git 歷史是永久的，commit 過之後就算刪檔，歷史裡還在
#   3. 這些資料放 Google Sheet 就夠了，沒有理由再放一份到 GitHub
airport.xls
*.xls
*.xlsx
*.csv
data/

# ── 憑證與金鑰 ───────────────────────────────────────
.clasprc.json
*.pem
*.key

# ── 系統與編輯器 ─────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# ── 暫存 ─────────────────────────────────────────────
node_modules/
tmp-*.js
*.tmp
__pycache__/
